define(function () {
	'use strict';

	var resources = {
		required: "Field is required",
		email: 'Field must have e-mail format',
		date: 'Field must have date format',
		integer: 'Field must be integer',
		maxLength: 'Length must be less than %maxLength%'
	};

	return resources;
});